import { Rubik, Noto_Serif } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  display: "swap",
});

export const noto = Noto_Serif({
  subsets: ["latin"],
  display: "swap",
});
